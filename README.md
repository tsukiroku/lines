```sh
USAGE:
    lines [OPTIONS]

OPTIONS:
    -d, --directory <DIRECTORY>    [default: ./]
    -f, --file <FILE>              
    -h, --help                     Print help information
    -i, --ignore <IGNORE>
```

```sh
lines

lines -i="myDir, myFile.txt"

lines -f="myFile.txt"
```